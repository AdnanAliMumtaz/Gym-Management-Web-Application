@model List<Member>


<table>
    <thead>
        <tr>
            <th>#</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Phone Number</th>
            <th>Date Joined</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var member in Model)
            {
                <tr>
                    <td>@member.MemberID</td>
                    <td>@member.MemberFirstName</td>
                    <td>@member.MemberLastName</td>
                    <td>@member.MemberEmail</td>
                    <td>@member.MemberPhoneNumber</td>
                    <td>@member.MemberDateJoined.ToString("yyyy-MM-dd")</td>
                    <td>
                        <a asp-controller="Members" asp-action="Edit" asp-route-id="@member.MemberID">Edit</a>
                        <a asp-action="Delete" asp-route-id="@member.MemberID">Delete</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="7">No members found</td>
            </tr>
        }
    </tbody>
</table>